import 'package:flutter/material.dart';

class ChannelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: Create Browse Page
    return Scaffold(
      appBar: AppBar(
        leading: IconButton(icon: null, onPressed: null),
      ),
      body: Text(' channel'),
    );
  }
}
